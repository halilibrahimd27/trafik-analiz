# Derin Öğrenme
# Apple Silicon (M-serisi) -> tensorflow-macos + Metal GPU hızlandırma
tensorflow-macos>=2.12.0; sys_platform == "darwin" and platform_machine == "arm64"
tensorflow-metal>=1.0.0; sys_platform == "darwin" and platform_machine == "arm64"
# Diğer tüm platformlar (Linux / Windows / Intel Mac) -> standart tensorflow
tensorflow>=2.12.0; sys_platform != "darwin" or platform_machine != "arm64"

# Veri işleme
numpy>=1.23.0
pandas>=2.0.0
Pillow>=9.5.0
opencv-python>=4.7.0

# Görselleştirme
matplotlib>=3.7.0
seaborn>=0.12.0

# Metrikler
scikit-learn>=1.2.0

# Web arayüzü
streamlit>=1.30.0

# Yardımcılar
tqdm>=4.65.0
